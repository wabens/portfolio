import React, { Component } from 'react';
import './App.css';

class App extends Component {
  // Renders the entire app on the DOM
  render() {
    return (
      <section>
        <p>Empty Page</p>
      </section>
    );
  }
}

export default App;
